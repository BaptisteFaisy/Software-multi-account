[defaults]
host_key_checking = True
interpreter_python = auto_silent
retry_files_enabled = False
timeout = 20

[ssh_connection]
pipelining = True
ssh_args = -o BatchMode=yes -o ServerAliveInterval=30 -o ServerAliveCountMax=4
