# Dans le conteneur, le service ecoute toutes les interfaces. Compose ne le
# publie toutefois que sur 127.0.0.1 du VPS.
CST_BIND=0.0.0.0:8080
CST_DATA_DIR=/srv/cst
CST_WORKSPACES_ROOT=/srv/cst/workspaces
CST_STATIC_DIR=/opt/codex-switch-terminal/dist
CST_PUBLIC_BASE_URL=http://127.0.0.1:8080
CST_ALLOWED_ORIGINS=http://127.0.0.1:8080
CST_ADMIN_TOKEN=replace-with-at-least-32-random-characters
CST_GIT_PAT=
CST_NODE_ID=vps
CST_NODE_LABEL=VPS
CST_NODE_CAPACITY=1
# Microsoft 365 (optionnel) : Outlook et agenda dans le chat. Les deux premieres
# valeurs activent l'integration, les deux suivantes sont facultatives (tenant
# par defaut "common", redirection deduite de CST_PUBLIC_BASE_URL). L'URI doit
# etre declaree a l'identique dans Entra ID, et en https des que le service n'est
# plus joint sur 127.0.0.1. Procedure detaillee : docs/microsoft-365.md.
# CST_MICROSOFT_CLIENT_ID=00000000-0000-0000-0000-000000000000
# CST_MICROSOFT_CLIENT_SECRET=replace-with-entra-client-secret
# CST_MICROSOFT_TENANT_ID=common
# CST_MICROSOFT_REDIRECT_URI=https://vps.example.net/api/microsoft/callback
# Facultatif aussi : ne changer que pour retirer une capacite au compte lie.
# CST_MICROSOFT_SCOPES=offline_access openid profile email User.Read Mail.Send Mail.Read Calendars.ReadWrite
# Configuration optionnelle pour la voix depuis le web/mobile du VPS.
# CST_VOICE_TRANSCRIPTION_MODE=remote
# CST_VOICE_TRANSCRIPTION_URL=https://gpu.example.net/stt/v1/audio/transcriptions
# CST_VOICE_TRANSCRIPTION_ACCELERATOR=gpu
# CST_VOICE_TRANSCRIPTION_API_KEY=replace-with-stt-token
# CST_VOICE_OLLAMA_URL=https://gpu.example.net/ollama
# CST_VOICE_OLLAMA_API_KEY=replace-with-ollama-token
